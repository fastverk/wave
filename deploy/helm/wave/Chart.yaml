apiVersion: v2
name: wave
description: |
  Scheduled cross-repo dependency automation: discover out-of-date external
  (3rd-party) dependencies across a forge group and drive in-flight internal
  version-bump cascades to completion.
type: application
# Chart version — bump on chart changes.
version: 0.2.0
# The wave version this chart deploys (the default image tag).
appVersion: "0.2.0"
home: https://github.com/fastverk/wave
sources:
  - https://github.com/fastverk/wave
keywords:
  - dependencies
  - renovate
  - gitlab
  - cascade
maintainers:
  - name: fastverk
